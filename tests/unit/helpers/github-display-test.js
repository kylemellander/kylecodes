import { githubDisplay } from '../../../helpers/github-display';
import { module, test } from 'qunit';

module('Unit | Helper | github display');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = githubDisplay(42);
  assert.ok(result);
});
