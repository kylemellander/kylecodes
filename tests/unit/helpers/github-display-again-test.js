import { githubDisplayAgain } from '../../../helpers/github-display-again';
import { module, test } from 'qunit';

module('Unit | Helper | github display again');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = githubDisplayAgain(42);
  assert.ok(result);
});
